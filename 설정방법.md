# sehyun03.github.io 저장소를 내 홈페이지로 만들기

친구분 말이 맞습니다. 이 저장소는 al-folio(Jekyll 테마) 기반이고, 내용은 전부
`_news/`, `_education/`, `_bibliography/papers.bib` 같은 **텍스트 파일**에 들어 있어서
VSCode로 열어 갈아끼우기만 하면 됩니다. HTML/CSS는 건드릴 필요 없습니다.

---

## 1. 내 GitHub 저장소 만들기

먼저 GitHub에서 **`<내아이디>.github.io`** 라는 이름으로 새 저장소를 만듭니다.
(Public, README 체크 해제 — 빈 저장소로 만드세요.)

Fork 대신 **복사**를 권합니다. Fork로 하면 "forked from sehyun03" 딱지가 붙고
Actions가 기본 비활성화라 한 번 더 켜야 합니다.

```bash
git clone https://github.com/sehyun03/sehyun03.github.io.git myhome
cd myhome
rm -rf .git                       # 원본 히스토리 제거
git init -b main
git add .
git commit -m "init"
git remote add origin https://github.com/<내아이디>/<내아이디>.github.io.git
git push -u origin main
```

`deploy.yml`이 `main`과 `master` 둘 다 인식하니 브랜치 이름은 아무거나 괜찮습니다.

---

## 2. 남의 자료 지우기

commit 하기 전에 정리하세요. 사진·CV·논문 이미지는 다른 사람 것입니다.

```
assets/pdf/cv_sehyun.pdf
assets/img/prof_sehyun_v1.jpg, prof_sehyun_v2.jpg, prof_sehyun_v4.jpeg, prof_namyup.jpg
assets/img/gwon2025enhancing.png, hwang2023active.png, hwang2023combating2.png,
           kim2022debias2.png, kim2023adaptive.png, kim2024active.jpeg, lee2024extreme.png
assets/img/publication_preview/ 안의 파일들
_posts/2023-10-25-research-interest.md
_news/ann_1.md ~ ann_16.md            (아래 6개로 교체)
_education/education_1.md, education_2.md
_experience/experience_*.md           (experience_1.old.md 포함)
_service/                             (오타로 만들어진 미사용 폴더, 통째로 삭제)
```

---

## 3. 내용 파일 갈아끼우기

이 폴더(`al-folio-content/`) 안의 파일들을 저장소의 **같은 경로에 그대로 덮어쓰기** 하면
CV 내용이 다 들어갑니다.

| 파일 | 홈페이지에서 보이는 곳 |
| --- | --- |
| `_config.yml` | 이름, 이메일, 사이트 주소, Scholar ID, favicon |
| `_pages/about.md` | 맨 위 자기소개 문단, 프로필 사진 파일명, 주소 |
| `_news/ann_*.md` | News (파일 하나 = 항목 하나, 날짜순 정렬) |
| `_education/education_*.md` | Education (번호 작을수록 오래된 것) |
| `_experience/experience_*.md` | Experience (번호 작을수록 오래된 것) |
| `_honors/honor_1.md` | Honors and Awards + Invited Talks |
| `_services/service_1.md` | Professional Services + Teaching |
| `_bibliography/papers.bib` | Publications |
| `_layouts/bib.html` | 논문 저자 목록에서 **내 이름에 밑줄** 치는 부분 |
| `_includes/header.html` | 상단 Curriculum Vitae 링크가 가리키는 PDF 경로 |

`_layouts/bib.html`은 놓치기 쉽습니다. 46번째 줄에 `author.first == "Sehyun"`이
하드코딩돼 있어서, 안 바꾸면 논문 목록에서 내 이름이 강조되지 않습니다.
(이미 `"Sangwoo"`로 바꿔뒀습니다.)

### 그다음 직접 넣을 것

- `_config.yml`의 `url:` → `https://<내아이디>.github.io`
- `assets/img/prof_sangwoo.jpg` → 프로필 사진
- `assets/pdf/cv_sangwoo.pdf` → CV PDF
- `assets/img/<bibkey>.png` → 논문 썸네일. 파일명은 `papers.bib`의 `img_path`와 맞추면 됩니다
- `papers.bib`의 `abstract={}`, `arxiv=`, `code=` → 초록과 링크
- `_news/ann_*.md`의 날짜 → 지금은 매월 1일로 넣어놨으니 실제 날짜로 수정

---

## 4. GitHub 설정 두 가지 (이거 안 하면 안 올라감)

이 저장소는 GitHub Actions가 Jekyll을 빌드해서 결과물을 `gh-pages` 브랜치에 밀어넣고,
GitHub Pages가 그 브랜치를 서빙하는 구조입니다. 그래서 설정이 두 군데 필요합니다.

**(1) Actions에 쓰기 권한 주기**

`Settings → Actions → General → Workflow permissions`
→ **Read and write permissions** 선택 → Save

개인 계정에서 새로 만든 저장소는 토큰이 기본 읽기 전용이라, 이걸 안 켜면
Action이 `gh-pages`에 push하다가 403으로 실패합니다.

**(2) Pages 소스 지정**

먼저 한 번 push해서 Actions가 돌고 `gh-pages` 브랜치가 생기게 한 다음,

`Settings → Pages → Build and deployment → Source`
→ **Deploy from a branch** → Branch: **gh-pages** / **`/ (root)`** → Save

몇 분 뒤 `https://<내아이디>.github.io` 로 접속됩니다.

Fork로 하셨다면 Actions 탭에 들어가서 워크플로를 한 번 활성화(**I understand my
workflows, go ahead and enable them**)해야 돌아갑니다.

---

## 5. 안 될 때 확인 순서

1. **Actions 탭** — deploy 워크플로가 초록색인지. 빨간색이면 로그에 원인이 있습니다
2. **403 / permission denied** — 위 4-(1) 안 한 것
3. **404** — `gh-pages` 브랜치가 생겼는지, Pages 소스가 거기로 잡혀 있는지
4. **CSS가 깨져 보임** — `_config.yml`의 `url:`이 아직 sehyun03으로 돼 있는 것
5. **Action은 성공인데 화면이 그대로** — Pages 재빌드가 안 걸린 경우. 아무 커밋이나
   하나 더 push하거나 Pages 설정에서 Save를 다시 눌러보세요

---

## 6. 로컬에서 미리 보고 싶다면 (선택)

저장소에 Dockerfile이 있어서 Ruby 설치 없이 볼 수 있습니다.

```bash
docker run --rm -v "$PWD:/srv/jekyll" -p 4000:4000 jekyll/jekyll:4 \
  bash -c "bundle install && bundle exec jekyll serve --host 0.0.0.0"
```

`http://localhost:4000` 에서 확인. 안 되면 그냥 push하고 Actions 결과를 봐도 됩니다.
